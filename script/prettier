#!/usr/bin/env bash

set -euo pipefail

PRETTIER_VERSION=3.8.3

if [[ ${1:-} == "-h" || ${1:-} == "--help" ]]; then
  if [[ $# -ne 1 ]]; then
    echo "Unexpected argument: $2" >&2
    echo "Run ./script/prettier --help" >&2
    exit 1
  fi
  echo "Usage: ./script/prettier [OPTIONS]"
  echo "Check or fix repository formatting."
  echo "Options:"
  echo "  --write     Write formatting changes."
  echo "  -h, --help  Show help."
  exit 0
fi
if [[ $# -gt 1 ]]; then
  echo "Unexpected argument: $2" >&2
  echo "Run ./script/prettier --help" >&2
  exit 1
fi
if [[ $# -eq 1 && $1 != "--write" ]]; then
  echo "Unexpected argument: $1" >&2
  echo "Run ./script/prettier --help" >&2
  exit 1
fi

if [[ "${1:-}" == "--write" ]]; then
  mode=(--list-different --write)
else
  mode=(--check)
fi

if ! command -v pnpm >/dev/null 2>&1; then
  echo "Missing required command: pnpm" >&2
  exit 1
fi

pnpm dlx "prettier@${PRETTIER_VERSION}" '**/*.{yml,yaml,json,jsonc,md,svg}' "${mode[@]}" || {
  status=$?
  if [[ $status -eq 1 && ${1:-} != "--write" ]]; then
    echo "Run ./script/prettier --write" >&2
  fi
  exit "$status"
}
