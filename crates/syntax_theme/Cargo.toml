[package]
name = "syntax_theme"
license.workspace = true
authors.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[lib]
name = "syntax_theme"
path = "src/syntax_theme.rs"

[features]
default = []
test = ["gpui/test-support"]

[dependencies]
gpui = { workspace = true }

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }
